@page "/"
@page "/{categoryurl}"
@inject ICategoryService CategoryService;
@inject IProductService ProductService
@inject ISyncLocalStorageService LocalStorage

@if (category != null)
{
    <h1>Here are our current selections of @category.Name:</h1>
    <ProductList />
}
else
{ <AuthorizeView>
    <NotAuthorized>
        <h1>Welcome to HalalOnTheGo - The ONLY halal butcher provider for our students</h1>
       
        <br>
        <p class = "italic-needs-signin">Hey there, hungry student! Ready to satisfy those taste buds? Dive into our meat selection tailored just for               you! Sign in quick with your university credentials - we've streamlined it for busy bees like you! Click the sign-up or sign-in option in              the navbar to purchase and let's get this culinary adventure started! </p>
        <br>
        <p>You are still free to browse our vast selection of premium halal butchered meats, with prices that are aimed directly at students only.</p>
        <ProductList/>
    </NotAuthorized>
    <Authorized>
        <h1>Welcome back to HalalOnTheGo, @LocalStorage.GetItemAsString("email")!</h1>
          <h2>The ONLY halal butcher provider for our students</h2>
    <p>We hope you find what you need.</p> 
        <ProductList/>
    </Authorized>
</AuthorizeView>
    
  
    
}
@code
{

    [Parameter]
    public string CategoryUrl { get; set; }

    private Category category = null;

    protected override async Task OnParametersSetAsync()
    {
      await ProductService.LoadProducts(CategoryUrl);

        if (CategoryUrl != null)
        {
            category = CategoryService.Categories.FirstOrDefault(c => c.Url.ToLower().Equals(CategoryUrl.ToLower()));
        }
        else
        {
            category = null;
        }
    }

}